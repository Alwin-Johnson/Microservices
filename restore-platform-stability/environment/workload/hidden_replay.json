{
  "name": "hidden_replay",
  "description": "Held-out variant of the failure replay used only by the verifier. Same qualitative shape as replay.json (a sustained burst above the trip threshold) but with different timing, rate, and seed, so a fix that hardcodes replay.json's exact numbers will not pass.",
  "duration_seconds": 260,
  "target_base_url": "http://gateway:8080",
  "phases": [
    {"label": "ramp", "start_s": 0, "end_s": 20, "requests_per_second": 5},
    {"label": "burst", "start_s": 20, "end_s": 200, "requests_per_second": 19},
    {"label": "cooldown", "start_s": 200, "end_s": 260, "requests_per_second": 5}
  ],
  "endpoints": [
    {"method": "POST", "path": "/checkout", "weight": 7, "payload_template": "checkout_small"},
    {"method": "GET", "path": "/orders/{order_id}", "weight": 3},
    {"method": "GET", "path": "/inventory/{sku}", "weight": 1}
  ],
  "seed": 9001
}
